source 'https://rubygems.org'

git_source(:github) { |repo_name| "https://github.com/#{repo_name}" }

# Specify your gem's dependencies in explain_pattern.gemspec
gemspec

gem 'regexp_parser'
gem 'terminal-table'
gem 'tree_support'
group :development, :test do
end
